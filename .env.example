# Copy this file to .env or set the variable in your shell.
OPENAI_API_KEY=your_dashscope_or_qwen_api_key
# Optional overrides. Bundled model weights are used when these are not set.
# UNET_AGENT_MODEL=models/weights/original_unet/best_unet.pt
# SEGMENTATION_WORKSPACE_ROOT=.
# UNET_RUNS_DIR=original_unet_project/runs
